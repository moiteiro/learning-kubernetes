apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: kuber-rs
  labels:
    app: kuber
    type: back-end
spec:
  template:
    metadata:
      name: kuber-pod
      labels:
        app: myapp
        type: back-end
    spec:
      containers:
        - name: kuber-container
          image: kuber:0.0.1
  replicas: 2
  selector:
    matchLabels:
      type: back-end
